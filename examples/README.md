# Examples

`task.yaml` is one `ratchetloop run`. `idea.md` is one `ratchetloop build`.
Replace `repo:` with a git checkout you own before a real run.

```bash
venv/bin/ratchetloop run examples/task.yaml --check --policy WORKFLOW.md
```
